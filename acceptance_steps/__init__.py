"""Cluster acceptance step definitions (behave ``--stage acceptance``).

These steps drive a DEPLOYED poker cluster over gRPC — see ``_client.py`` for
the transport and ``_world.py`` for per-scenario isolation. The shared feature
files live in ``angzarr-project/features/example/acceptance/``.
"""
