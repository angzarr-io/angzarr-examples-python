# Code generated by angzarr codegen python. DO NOT EDIT.
# source: io/angzarr/examples/v1/components.proto

from typing import Optional, Protocol

import angzarr_router_ffi as _az
from io.angzarr.v1 import types_pb2 as _t
from io.angzarr.examples.v1 import buy_in_pb2 as _m0
from io.angzarr.examples.v1 import components_pb2 as _m1
from io.angzarr.examples.v1 import hand_pb2 as _m2
from io.angzarr.examples.v1 import player_pb2 as _m3
from io.angzarr.examples.v1 import table_pb2 as _m4

class OutputProjectorHandler(Protocol):
    """Strict business seam for the OutputProjector projector."""

    def cards_dealt(self, projection: _m1.OutputProjection, event: _m2.CardsDealt) -> None: ...
    def blind_posted(self, projection: _m1.OutputProjection, event: _m2.BlindPosted) -> None: ...
    def action_taken(self, projection: _m1.OutputProjection, event: _m2.ActionTaken) -> None: ...
    def community_cards_dealt(self, projection: _m1.OutputProjection, event: _m2.CommunityCardsDealt) -> None: ...
    def cards_revealed(self, projection: _m1.OutputProjection, event: _m2.CardsRevealed) -> None: ...
    def cards_mucked(self, projection: _m1.OutputProjection, event: _m2.CardsMucked) -> None: ...
    def showdown_started(self, projection: _m1.OutputProjection, event: _m2.ShowdownStarted) -> None: ...
    def pot_awarded(self, projection: _m1.OutputProjection, event: _m2.PotAwarded) -> None: ...
    def hand_complete(self, projection: _m1.OutputProjection, event: _m2.HandComplete) -> None: ...
    def player_timed_out(self, projection: _m1.OutputProjection, event: _m2.PlayerTimedOut) -> None: ...
    def player_seated(self, projection: _m1.OutputProjection, event: _m0.PlayerSeated) -> None: ...
    def player_registered(self, projection: _m1.OutputProjection, event: _m3.PlayerRegistered) -> None: ...
    def funds_deposited(self, projection: _m1.OutputProjection, event: _m3.FundsDeposited) -> None: ...
    def funds_withdrawn(self, projection: _m1.OutputProjection, event: _m3.FundsWithdrawn) -> None: ...
    def funds_reserved(self, projection: _m1.OutputProjection, event: _m3.FundsReserved) -> None: ...
    def table_created(self, projection: _m1.OutputProjection, event: _m4.TableCreated) -> None: ...
    def player_joined(self, projection: _m1.OutputProjection, event: _m4.PlayerJoined) -> None: ...
    def player_left(self, projection: _m1.OutputProjection, event: _m4.PlayerLeft) -> None: ...
    def hand_started(self, projection: _m1.OutputProjection, event: _m4.HandStarted) -> None: ...
    def hand_ended(self, projection: _m1.OutputProjection, event: _m4.HandEnded) -> None: ...
    def finish(self, projection: _m1.OutputProjection, events: _t.EventBook) -> _t.Projection: ...

def new_output_projector_dispatch(handler: OutputProjectorHandler) -> _az.ProjectorDispatch:
    dispatch = _az.ProjectorDispatch("OutputProjector", lambda: _m1.OutputProjection())
    dispatch.for_domains("hand")
    def _on_cards_dealt(projection, event_any):
        event = _m2.CardsDealt()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.cards_dealt(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.CardsDealt", _on_cards_dealt)
    def _on_blind_posted(projection, event_any):
        event = _m2.BlindPosted()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.blind_posted(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.BlindPosted", _on_blind_posted)
    def _on_action_taken(projection, event_any):
        event = _m2.ActionTaken()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.action_taken(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.ActionTaken", _on_action_taken)
    def _on_community_cards_dealt(projection, event_any):
        event = _m2.CommunityCardsDealt()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.community_cards_dealt(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.CommunityCardsDealt", _on_community_cards_dealt)
    def _on_cards_revealed(projection, event_any):
        event = _m2.CardsRevealed()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.cards_revealed(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.CardsRevealed", _on_cards_revealed)
    def _on_cards_mucked(projection, event_any):
        event = _m2.CardsMucked()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.cards_mucked(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.CardsMucked", _on_cards_mucked)
    def _on_showdown_started(projection, event_any):
        event = _m2.ShowdownStarted()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.showdown_started(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.ShowdownStarted", _on_showdown_started)
    def _on_pot_awarded(projection, event_any):
        event = _m2.PotAwarded()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.pot_awarded(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.PotAwarded", _on_pot_awarded)
    def _on_hand_complete(projection, event_any):
        event = _m2.HandComplete()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.hand_complete(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.HandComplete", _on_hand_complete)
    def _on_player_timed_out(projection, event_any):
        event = _m2.PlayerTimedOut()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.player_timed_out(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.PlayerTimedOut", _on_player_timed_out)
    def _on_player_seated(projection, event_any):
        event = _m0.PlayerSeated()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.player_seated(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.PlayerSeated", _on_player_seated)
    def _on_player_registered(projection, event_any):
        event = _m3.PlayerRegistered()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.player_registered(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.PlayerRegistered", _on_player_registered)
    def _on_funds_deposited(projection, event_any):
        event = _m3.FundsDeposited()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.funds_deposited(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.FundsDeposited", _on_funds_deposited)
    def _on_funds_withdrawn(projection, event_any):
        event = _m3.FundsWithdrawn()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.funds_withdrawn(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.FundsWithdrawn", _on_funds_withdrawn)
    def _on_funds_reserved(projection, event_any):
        event = _m3.FundsReserved()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.funds_reserved(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.FundsReserved", _on_funds_reserved)
    def _on_table_created(projection, event_any):
        event = _m4.TableCreated()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.table_created(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.TableCreated", _on_table_created)
    def _on_player_joined(projection, event_any):
        event = _m4.PlayerJoined()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.player_joined(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.PlayerJoined", _on_player_joined)
    def _on_player_left(projection, event_any):
        event = _m4.PlayerLeft()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.player_left(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.PlayerLeft", _on_player_left)
    def _on_hand_started(projection, event_any):
        event = _m4.HandStarted()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.hand_started(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.HandStarted", _on_hand_started)
    def _on_hand_ended(projection, event_any):
        event = _m4.HandEnded()
        try:
            event.ParseFromString(event_any.value)
        except Exception as exc:
            raise _az.any_decode_error(event_any.type_url, exc)
        handler.hand_ended(projection, event)
    dispatch.on_event("io.angzarr.examples.v1.HandEnded", _on_hand_ended)
    dispatch.finish(handler.finish)
    return dispatch

def register_output_projector(router: _az.Router, handler: OutputProjectorHandler) -> None:
    router.register_projector(new_output_projector_dispatch(handler))

